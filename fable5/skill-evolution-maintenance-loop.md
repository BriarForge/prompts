# Skill Evolution & Maintenance Loop

**Category:** self-improvement  
**Tags:** [skills, maintenance, evolution, hermes-prime]

```text
Fable 5 one-shot mode (high effort, long-run scaffolding + git workflow enabled):

Objective: Create a self-sustaining skill improvement system that detects when loaded skills are stale, incomplete, or mismatched to Fable-class models, then generates precise patches or new skills. The system must minimize manual curator work while maintaining high signal.

Ground the system in these verified skill directories:
- KovaForge canonical skills: /Users/mike/Projects/KovaForge/skills/
- Aoife profile skills: ~/.hermes/profiles/aoife/skills/

Use Fable 5 scaffolding:
1. Rubric for "skill quality".
2. Persistent memory + verification loops.
3. Decomposition.

Git workflow (mandatory, use wrappers only):
- After every major section or significant change: progressively commit with `git-aoife commit -m "msg"`
- At the very end: final commit + `git-aoife push`

Done looks like: An automated detection + proposal workflow (with human approval gate only on final merge) plus the first three skill patches it would surface. Verify by applying it to the current skill set and confirming it would have caught at least two real maintenance issues that currently require explicit user intervention. End with rubric self-score.
```

## Notes
- After difficult tasks, offer to save learnings as skills.
- Patch skills immediately when issues are found.