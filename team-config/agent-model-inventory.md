---
tags: [agents, models, config, inventory]
category: meta
version: 1
---

# Agent Model Inventory

For each team member of yours, including yourself, list the provider and model version by reading the settings directly from the config file.

# Notes
- Always read live config (never use memory).
- Output format: `Agent Name — provider/model-version`
- Report primary + fallback if present.
